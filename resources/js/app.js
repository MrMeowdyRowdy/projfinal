import './bootstrap';
import { createApp } from 'vue';

import ComponentA from './components/ComponentA.vue';

createApp(ComponentA).mount('#componente-a');

// const app = createApp({});

// app.component('component-a', ComponentA);

// app.mount("#app");